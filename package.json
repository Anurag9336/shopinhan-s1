{
  "name": "shopinhand-vercel-functions",
  "description": "Serverless API functions for ShopInHand (place-order, create-razorpay-order, sitemap) — runs on Supabase (Postgres) + Vercel, fully free tier, no card required.",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "razorpay": "^2.9.4"
  }
}
